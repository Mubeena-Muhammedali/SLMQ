<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        OWL client action — no form/transient record needed.
        The JS component fetches its own data via ORM.
    -->
    <record id="action_garage_dashboard" model="ir.actions.client">
        <field name="name">Dashboard</field>
        <field name="tag">orchid_garage_dashboard</field>
    </record>

</odoo>
