<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!--
        These menu items extend the existing Garage menu tree.
        They assume that menu_garage_root is already defined in
        orchid_garage/views/menu.xml (which it is).
    -->

    <!-- ── DASHBOARD (sequence=0, appears first) ────────────────────────── -->
    <menuitem
        id="menu_garage_dashboard"
        name="Dashboard"
        parent="orchid_garage.menu_garage_root"
        action="action_garage_dashboard"
        sequence="0"/>

</odoo>
