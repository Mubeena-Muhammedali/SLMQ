<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <!-- ============================================================
         REPORT ACTION — Adds "Remittance Advice" button to Payment
    ============================================================ -->
    <record id="action_report_remittance_advice" model="ir.actions.report">
        <field name="name">Remittance Advice</field>
        <field name="model">account.payment</field>
        <field name="report_type">qweb-pdf</field>
        <field name="report_name">remittance_advice.remittance_advice_document</field>
        <field name="report_file">remittance_advice.remittance_advice_document</field>
        <field name="print_report_name">'Remittance Advice - %s' % (object.name)</field>
        <field name="binding_model_id" ref="account.model_account_payment"/>
        <field name="binding_type">report</field>
        <field name="paperformat_id" ref="invoice_template.paperformat_tax_invoice"/>
    </record>

</odoo>
