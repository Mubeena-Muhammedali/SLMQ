<?xml version="1.0" encoding="UTF-8"?>
<odoo>

    <record id="sale_order_report_action_custom"
            model="ir.actions.report">

        <field name="name">Quotation</field>

        <field name="model">sale.order</field>

        <field name="report_type">qweb-pdf</field>

        <field name="report_name">invoice_template.sale_order_document</field>

        <field name="report_file">invoice_template.sale_order_document</field>

        <field name="binding_model_id" ref="sale.model_sale_order"/>

        <field name="binding_type">report</field>

        <field name="paperformat_id" ref="invoice_template.paperformat_tax_invoice"/>

    </record>

</odoo>