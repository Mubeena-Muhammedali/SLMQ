<?xml version="1.0" encoding="UTF-8"?>
<odoo>

	<data noupdate="0">
		<record id="od_attendance_mail_template" model="mail.template">
			<field name="name">Attendance Email Template</field>
			<field name="subject">Attendance Reminder</field>
			<field name="email_to">{{ctx['email_to']}}</field>
			<field name="email_cc">{{ctx['email_cc']}}</field>
			<field name="model_id" ref="hr_attendance.model_hr_attendance"/>
			<field name="auto_delete" eval="False"/>
			<field name="body_html" type="html">
				<div style="margin: 0px; padding: 0px;">
                    <p style="margin: 0px; padding: 0px;">
                        Hello,
                        <span t-if="ctx.get('content')" t-out="ctx.get('content')"/>
				 		<br/><br/>
					    
				 	</p>
				 	<br/>
					<div style="margin: 16px 0px 16px 0px;">
                        <a t-attf-href="{{ctx['url']}}"
                            style="background-color: #875A7B; padding: 8px 16px 8px 16px; text-decoration: none; color: #fff; border-radius: 5px; font-size:13px;">
                            <span t-if="ctx.get('button_name')" t-out="ctx.get('button_name')"/>
                        </a>
                    </div>		 	
                    <br/><br/>
					<pre>
						Auto Generated Email
					</pre>
				</div>
			</field>
		</record>
    </data>

    <data noupdate="1">

	    <record id="od_attendance_login_reminder" model="ir.cron">
	        <field name="name">Login Reminder</field>
	        <field name="interval_number">1</field>
	        <field name="interval_type">days</field>
	        <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
	        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
	        <field name="code">model.od_login_reminder_email()</field>
	        <field name="state">code</field>
	    </record>
		
		<record id="od_attendance_logout_reminder" model="ir.cron">
	        <field name="name">Logout Reminder</field>
	        <field name="interval_number">1</field>
	        <field name="interval_type">days</field>
	        <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')"/>
	        <field name="model_id" ref="hr_attendance.model_hr_attendance"/>
	        <field name="code">model.od_logout_reminder_email()</field>
	        <field name="state">code</field>
	    </record>
	</data>
</odoo>