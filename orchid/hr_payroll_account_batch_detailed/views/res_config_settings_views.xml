<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="od_res_config_settings_view_form_inherit" model="ir.ui.view">
        <field name="name">od.res.config.settings.view.form.inherit</field>
        <field name="model">res.config.settings</field>
        <field name="inherit_id" ref="hr_payroll_account.res_config_settings_view_form"/>
        <field name="arch" type="xml">
            <xpath expr="//setting/field[@name='batch_payroll_move_lines']" position="after">
                <setting string="Batch Account Move Lines (Detailed by Employee)"
                         help="Enable this option to group all the payslips of a period into a single journal entry, while still keeping a separate account line per employee for each salary rule. Takes priority over 'Batch Account Move Lines' if both are enabled.">
                    <field name="od_batch_payroll_move_lines_detailed"/>
                </setting>
            </xpath>
        </field>
    </record>
</odoo>
