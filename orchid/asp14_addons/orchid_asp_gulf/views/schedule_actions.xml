<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!-- to check if contracts are expired -->
    <record id="od_ir_cron_auto_expire_asp_contracts" model="ir.cron">
        <field name="name">Expired Contracts</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <!-- <field name="nextcall" eval="(DateTime.now().replace(hour=2, minute=0) + timedelta(days=1)).strftime('%Y-%m-%d %H:%M:%S')" /> -->
        <field name="doall" eval="False"/>
        <field name="model_id" ref="model_od_asp_contract"/>
        <field name="code">model.button_expire()</field>
        <field name="state">code</field>
    </record>

    <!-- to check if contract Lines are expired -->
    <record id="od_ir_cron_auto_expire_asp_contract_lines" model="ir.cron">
        <field name="name">Expired Contract Lines</field>
       <!--  <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="doall" eval="False"/>
        <field name="model_id" ref="model_od_asp_contract_line"/>
        <field name="code">model.button_expire()</field>
        <field name="state">code</field> -->
        <field name="model_id" ref="model_od_asp_contract_line"/>
        <field name="state">code</field>
        <field name="code">model.button_expire()</field>
        <field name="interval_number">1</field>
        <field name="interval_type">days</field>
        <field name="numbercall">-1</field>
        <field name="doall" eval="False"/>
        <field name="user_id" ref="base.user_root"/>
    </record>
</odoo>